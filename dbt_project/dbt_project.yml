name: 'order_analytics'
version: '1.0.0'
config-version: 2

profile: 'order_analytics'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  order_analytics:
    staging:
      +schema: analytics
      +materialized: view
    intermediate:
      +schema: analytics
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table